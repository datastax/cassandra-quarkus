datastax-java-driver {
  basic.load-balancing-policy.local-datacenter = non existing # to validate that it is overriden by the application-cassandra-client.properties
  basic.request.timeout = 20 seconds # to validate that it has priority over reference.conf and over application.json
}